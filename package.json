{
  "name": "evolve-web",
  "version": "1.0.0",
  "description": "Evolve Poker — site vitrine",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "test": "node --test"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
